import React, { Component } from 'react'
import PropTypes from 'prop-types'

import { Container } from './styled'

class AddVisit extends Component {
  render() {
    return (
      <Container>
        Add Visit
      </Container>
    )
  }
}

AddVisit.displayName = 'AddVisit'

AddVisit.propTypes = {}

export default AddVisit
